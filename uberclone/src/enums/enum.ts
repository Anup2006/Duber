export enum UserRole {
  RIDER = "RIDER",
  DRIVER = "DRIVER",
  ADMIN = "ADMIN"
}

export enum RideStatus {
  REQUESTED = "REQUESTED",
  ACCEPTED = "ACCEPTED",
  STARTED = "STARTED",
  COMPLETED = "COMPLETED",
  CANCELLED = "CANCELLED"
}

export enum PaymentStatus {
  PENDING = "PENDING",
  SUCCESS = "SUCCESS",
  FAILED = "FAILED"
}

export enum VehicleType {
  BIKE = "BIKE",
  AUTO = "AUTO",
  CAR = "CAR"
}